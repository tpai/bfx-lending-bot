{
  "name": "bfx-bot",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "node index.js",
    "dev": "NODE_ENV=development node index.js",
    "build": "rm -rf *.zip && zip -r ${PWD##*/}_$(date +%s).zip . -x *.git* -x *.zip -x .env"
  },
  "dependencies": {
    "crypto-js": "^4.0.0",
    "dayjs": "^1.8.32",
    "dotenv": "^8.2.0",
    "node-fetch": "^2.6.0",
    "query-string": "^6.13.1"
  }
}
